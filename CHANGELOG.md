# 更新日志

本文件记录每个版本的显著改动。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.2.0] - 未发布

这一版重点修共识与网络的正确性、Web 端的安全性，并补上比特币最核心但此前完全缺失的几个机制。

> **升级提示**：`halving_interval` 和 `coinbase_maturity` 属于共识参数，会改变 `chain_params_hash`。
> 同一个课堂网络里的所有节点必须一起升级，旧版本节点不会加入新网络。数据库会自动迁移，
> 不需要手动重置。

### 修复

- **分叉后网络不再永久裂开。** 此前 `add_block` 只接受 `prev_hash` 等于当前链头的区块，
  其余一律丢弃，且没有任何回补路径。两个节点同时挖到同一高度就会永久分成两条链，
  只能靠人工点「同步区块」。现在无法连接的区块会进入孤块池，并主动向对方请求整条链。
- **最长链规则改为累计工作量。** `replace_with_chain` 原本比较区块数量，
  开了自动难度以后这是错的：11 个低难度区块会顶掉 10 个高难度区块。
  现在按 `2^256 / (target+1)` 累加比较，且只在全部区块通过 PoW 校验之后才比较，
  避免伪造的难度声明。工作量相同时保留先看到的链。
- **`sync_interval_seconds` 真正生效。** 这个配置项从第一个提交起就写在所有配置文件里，
  但代码里从来没有读过它。现在每个周期会 ping 存活节点、重连已知但断开的节点，
  并从工作量更大的节点拉取链。心跳同时携带对方的高度与累计工作量，
  否则这些值会停留在握手那一刻，节点永远发现不了别人已经领先。
- **无 IPv6 主机上的崩溃。** `_route_ip` 在 `try` 之外创建 socket，
  在没有 IPv6 栈的机器上 `socket.socket(AF_INET6, ...)` 直接抛异常，
  连带打垮「入网」页、状态接口和教师页。
- **`POST /api/peers` 不再卡住。** 连接对端没有超时，填错局域网地址会让请求
  挂到操作系统的 SYN 超时（约两分钟），浏览器按钮一直转圈。
- **区块浏览器翻页 off-by-one。** 最后一页正好满时「下一页」仍可点，会翻到空页。
- 修正 `max_difficulty` 回退值与默认配置不一致、三处版本号不同步、
  README 中混入的一个 PowerShell 转义符。

### 安全

- **Web API 增加写操作鉴权。** 此前所有接口都没有任何认证，其中
  `POST /api/transactions` 会用本节点私钥签名——局域网上任何一台设备
  都能用一条 curl 把同学钱包里的币转走，`POST /api/chain/reset` 能清空别人的链。
  `?administrator=true` 只是前端隐藏按钮，服务端从未校验。
  现在读接口保持开放（这正是课堂演示需要的），写接口要求请求来自本机，
  或携带节点的 admin token（`X-Admin-Token` 头或 `?token=`）。
  token 存在数据库同级目录，不进版本库，启动时打印一次。
- **修复前端 XSS。** 控制台所有表格都用 innerHTML 字符串拼接且零转义，
  而节点名、参数不匹配原因、安全事件文本都是别的节点通过 P2P 送来的。
  同学把节点名改成 `<img src=x onerror=...>` 就能在全班浏览器里执行脚本，
  首当其冲的正是用来展示攻击的「安全」页。现在全部改用 createElement + textContent，
  并有测试防止回退。
- 新增节点封禁/解封与移除，安全页每条告警旁可直接封禁来源节点。
- 支持配置 `trust_loopback_admin` 与 `require_admin_for_writes`，
  需要完全开放的课堂可以关掉鉴权。

### 新增

- **减半与总量上限。** 区块奖励此前是配置里的固定值，永远不变，
  比特币最标志性的两个机制在这里完全不存在。新增 `halving_interval`，
  纪元边界与比特币一致（`halvings = height / interval`）。
  因为创世块不发币，总量上限刻意不是整数——比特币的 20,999,999.9769 也是同样的道理。
- **coinbase 成熟期。** `coinbase_maturity` 控制挖矿奖励需要被埋多深才能花。
  单块校验、链替换、内存池准入和打包四处都会执行这条规则。
- **中位时间过去（MTP）。** 区块时间戳不得早于最近 11 个区块的中位数，
  否则矿工可以往回填时间戳来压缩难度调整窗口、操纵难度。
  与比特币的「严格大于」不同，这里用「大于等于」：低难度课堂链每秒能出几百个区块，
  严格规则会让时间戳自行向前棘轮，几分钟内漂到未来数小时，
  最终触发两小时未来块限制、节点开始拒绝自己挖出的块。
- **Merkle 包含性证明。** `merkle.py` 原本只算根。新增 `merkle_proof` /
  `verify_merkle_proof`，以及 `GET /api/proof/{tx_id}`，
  控制台可以逐步展示证明路径——1024 笔交易只要 10 个哈希，这就是 SPV。
- **交易记录与搜索。** 此前发完交易只有一个 2.8 秒的提示，tx_id 就永远消失了，
  浏览器也只能按区块高度或 hash 查。现在有交易历史页（方向、确认数、待确认）、
  `GET /api/transactions`、`GET /api/search`（高度 / 区块 hash / 交易 ID / 地址通吃），
  以及所有截断哈希点击即复制。
- **图表页。** 出块间隔与目标对比、难度曲线、估算算力、累计发行量、
  每块奖励与手续费、内存池费率分布。全部用 canvas 绘制，没有引入任何依赖。
- **内存池费率市场。** 此前池满就拒收一切新交易，不看出价，也从不过期。
  现在满了会淘汰费率最低的交易，支持按费率排序打包、过期清理、
  RBF 手续费替换和 `min_relay_fee`。
- **多钱包。** 生成新钱包不再让旧钱包在界面上消失，可切换、可导出/导入私钥。
- **真正的二维码。** 「入网码」此前是用 xorshift 伪随机数填出来的装饰图案，
  任何扫码器都读不出来。换成完整的 ISO/IEC 18004 编码器
  （字节模式、ECC M、版本 1–10，含 Reed-Solomon 与掩码评分），
  因为课堂局域网没有外网，编码器随仓库内置而非走 CDN。
- 深色模式、移动端布局、tablist 语义与键盘导航、焦点框、跳转链接。
- 页面深链接（`#charts` 等），刷新不再回到挖矿页。
- 交易 / 节点 / 安全事件导出 CSV。
- 教师页自动刷新；实验任务改由服务端下发，老师可用 `lab_tasks.json` 覆盖，
  不用再改 JavaScript。

### 变更

- 状态值改为语言无关的标识符（`connected` / `param_mismatch` / `mining` 等），
  此前中文显示字符串被直接存进 SQLite、通过 P2P 传输并用 `==` 比较，
  改个文案就会破坏节点筛选逻辑。旧数据库会自动迁移。
- WebSocket 只推送发生变化的部分，此前每秒把整个状态对象
  （含 300 条日志缓冲）推给每一个打开的页面。
- FastAPI 的 `on_event` 迁移到 lifespan。
- `blocks` 表新增 `chain_work` 列，`peers` 表新增 `chain_work` / `tip_hash` 列，
  新增 `banned_peers` 表，并为交易查询补充索引。均自动迁移。

### 工程

- GitHub Actions CI：Python 3.10 / 3.11 / 3.12 上跑 ruff 与 pytest，
  外加一个启动并挖矿的冒烟测试。
- 测试从 34 个增加到 149 个，覆盖共识、发行、成熟期、Merkle 证明、
  内存池策略、HTTP 鉴权、前端安全静态检查，以及**双节点真实 socket 集成测试**
  ——分叉自愈这类问题只有在两个节点交互时才会出现。
- 新增 `Dockerfile` 与 `docker-compose.yml`（一条命令起三个已互联的节点）。
- 新增 LICENSE（MIT）与本更新日志；此前更新日志混在 README 里。

- **真实区块头与 nBits（展示用）。** 新增 `GET /api/blocks/{id}/header`，
  把区块按比特币真实规则序列化成 80 字节，逐字段拆开显示：小端序整数、
  反转字节序的哈希、以及 nBits 的「1 字节指数 + 3 字节尾数」压缩编码。
  这一层**不参与共识**——模拟器的区块 hash 仍是 canonical JSON 的双 SHA256，
  改掉会让所有现存课堂数据库失效，而 JSON 形式在白板上更好讲。
  序列化器用比特币创世块做校验：80 字节逐字节一致，且双 SHA256 反转后
  等于人尽皆知的 `000000000019d6689c085ae165831e934ff763ae46a2a6c172b3f1b60a8ce26f`。
- **分叉可见。** 新增 `GET /api/tips`，浏览器页会在出现竞争区块时显示横幅，
  列出没能接上链头的孤块。此前分叉在界面上完全不可见——控制台只显示赢家。

### 明确未做

- **仍然是账户余额模型，不是 UTXO。** 这是一次破坏性重写：交易结构、签名内容、
  余额计算、区块校验、内存池和前端都要改，应当单独开一个 PR，而不是塞进这一批。
  README 的「当前限制」里已明确标注。
- 分叉处理仍是「整条链替换」，不保存多个分支、没有部分重组。
  孤块池让节点能自愈并把分叉显示出来，但重组本身还是整链替换。
- 私钥仍以明文保存。这是有意的教学取舍，README 已说明。
- **挖矿速度没有改。** `miner.py` 里每个 nonce 之后的 `await asyncio.sleep(0.001)`
  是刻意的教学节流，让学生能看清 nonce 逐个递增。代码里已加注释说明，
  避免以后被当成 bug「优化」掉。

## [0.1.1]

- 节点身份与连接校验：随机节点名，连接时检查节点名、钱包地址与监听地址。
- 难度机制改为二进制前导零位数，每次调整 1 位。
- 新增「安全」页，显示非法交易、非法区块与异常同步。
- 种子节点的 Web 控制台默认只绑定 127.0.0.1。

## [0.1.0]

- 首个版本：账户余额模型、SQLite 存储、ECDSA secp256k1 签名、
  TCP JSON Lines P2P、本地 Web 控制台。
